spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    password: root
    username: root
    url: jdbc:mysql://localhost:3306/persondata
    driver-class-name: com.mysql.cj.jdbc.Driver
  liquibase:
    change-log: liquibase-changeLog.xml